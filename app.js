const socket=io(); const $=id=>document.getElementById(id); let myId=null,state=null; const points=[];
function buildBoard(){const b=$("board");b.innerHTML="";for(let i=0;i<28;i++){const a=-Math.PI/2+Math.PI*2*i/28,x=50+40*Math.cos(a),y=50+40*Math.sin(a);points.push({x,y});const e=document.createElement("div");e.className="node";e.style.left=x+"%";e.style.top=y+"%";e.textContent=i+1;b.appendChild(e)}const f=document.createElement("div");f.className="finish";f.textContent="완주";b.appendChild(f)}
function esc(v){return String(v).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]))}
function render(s){state=s;const me=s.players.findIndex(p=>p.id===myId);$("roomInfo").textContent=`방 코드: ${s.code} · ${s.players.length}/4명`;$("copy").hidden=false;$("players").innerHTML=s.players.map((p,i)=>`<div class="player" style="border-left-color:${p.color}">${i===s.turn&&s.started?"▶ ":""}${esc(p.name)}</div>`).join("");$("status").textContent=s.started?`현재 차례: ${s.players[s.turn]?.name||"-"}`:"2명 이상 모이면 게임을 시작할 수 있습니다.";$("roll").disabled=!s.started||me!==s.turn;$("start").disabled=s.players.length<2||s.started;$("restart").disabled=!s.started;$("history").innerHTML=s.history.slice().reverse().map(x=>`<div>• ${esc(x)}</div>`).join("");document.querySelectorAll(".token").forEach(x=>x.remove());s.players.forEach(p=>p.pieces.forEach((pos,k)=>{if(pos<=0||pos>=29)return;const q=points[(pos-1)%28],t=document.createElement("div");t.className="token";t.style.background=p.color;t.style.left=`calc(${q.x}% + ${(k%2)*12}px)`;t.style.top=`calc(${q.y}% + ${Math.floor(k/2)*12}px)`;$("board").appendChild(t)}))}
function pname(){return $("name").value.trim()||"플레이어"}
$("create").onclick=()=>socket.emit("createRoom",{name:pname()});$("join").onclick=()=>socket.emit("joinRoom",{name:pname(),code:$("room").value});$("start").onclick=()=>socket.emit("startGame");$("roll").onclick=()=>socket.emit("roll");$("restart").onclick=()=>socket.emit("restartGame");
$("copy").onclick=async()=>{const url=`${location.origin}/?room=${encodeURIComponent(state.code)}`;try{await navigator.clipboard.writeText(url);$("copy").textContent="복사 완료!";setTimeout(()=>$("copy").textContent="초대 링크 복사",1200)}catch{prompt("이 링크를 복사하세요:",url)}};
socket.on("connect",()=>myId=socket.id);socket.on("state",render);socket.on("errorMessage",m=>alert(m));const r=new URLSearchParams(location.search).get("room");if(r)$("room").value=r.toUpperCase();buildBoard();
